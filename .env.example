# gate-dex-cli 环境变量配置示例
# 复制此文件为 .env 后按需填值；所有变量都可不填，留空时走源码 prod 默认值
#
# 用法：
#   cp .env.example .env
#   # 编辑 .env 取消注释并填入所需 URL

# ── AI 网关统一接入 ────────────────────────────────────────
# BIZ_WALLET / BW_SERVICE / WALLET_SERVICE / MARKET_TOKEN / DATA_API 共 5 个服务
# 默认走同一个 AI 网关域名，由网关按 path 前缀分发到各业务 LB。
#   - AI_GATEWAY_URL 整体覆盖这 5 个服务的网关域名
#   - 下方各服务单独的 *_URL 优先级更高，可精确覆盖 / 降级单个服务
#   - GV_URL / CDN_DOMAINS 不经 AI 网关，维持直连
# 默认 prod: https://api.gate-cli.com
# 测试环境:  https://api-test.gate-cli.com
# AI_GATEWAY_URL=

# ⚠️ BIZ_WALLET_URL vs BW_SERVICE_URL 注意事项（仍是两个独立变量）
#   - BIZ_WALLET_URL  = 登录前置流程入口（OAuth Device Flow + merchant 凭证）
#   - BW_SERVICE_URL  = 登录后业务调用（钱包/转账/签名）
#   接入 AI 网关后两者 base 相同，由网关按子路径二次分发；保留两个变量是为了
#   能独立覆盖 / 降级单个服务。详细说明见 cli/src/core/api-client.ts 头部注释。

# ── GV (Gate Verify) API ────────────────────────────────────
# 不经 AI 网关，维持直连。
# 默认 prod: https://api.web3gate.io/api/plug/v1/web3-gv-api
# GV_URL=

# ── 动态域名 CDN 候选列表（逗号分隔；覆盖默认 prod 列表） ──────────
# 不经 AI 网关，维持直连。
# 默认 prod: 源码内置 7 个 prod CDN host（freshmarkethome / freshmarketpage /
#           gateweb3.io / ldd678 / web3gate.cc / web3gate.io / web3-wallet-cdn-prod）
# CDN_DOMAINS=

# ── web3_domain host 列表（逗号分隔；设置后跳过 CDN 拉取直接使用） ──
# 默认 prod: 留空，走 CDN_DOMAINS 动态拉取
# 仅 pre 环境需要硬编码：
#   pre: http://pre-api.ldd710.com,http://pre-api.ldd711.com,http://pre-api.ldd712.com
# WEB3_DOMAIN_HOSTS=

# ── WALLET_SERVICE_URL ── gateio-service-web3-wallet-service（balance 命令用） ──
# 默认走 AI 网关（= {AI_GATEWAY_URL}/gateio-service-web3-wallet-service）。
# 仅在需绕过网关单服务直连时填写。
# 单服务直连 prod: https://web3-wallet-service-prod.gateweb3.cc
# WALLET_SERVICE_URL=

# ── BW_SERVICE_URL ── web3-business-wallet（登录后业务调用） ──
# 用途：钱包/转账/签名等所有业务接口
# 默认走 AI 网关（= {AI_GATEWAY_URL}/web3-business-wallet）。
# 单服务直连仅有内网入口: http://web3-ingress-prod.gateweb3.io/web3-business-wallet
# BW_SERVICE_URL=

# ── MARKET_TOKEN_URL ── gateio-service-web3-token-chain（行情 / Token / Swap） ──
# 默认走 AI 网关（= AI_GATEWAY_URL）。仅在需绕过网关单服务直连时填写。
# 单服务直连 prod: https://apipro-prod.gateweb3.cc
# 另有抗封禁域名：apipro.gateweb3.cc / api.freshmarketpage.com /
#                apipro.freshmarkethome.com / apipro.w3-api.com 等
# MARKET_TOKEN_URL=

# ── DATA_API_URL ── web3-data-api（Token 详情 / 安全审计 / 排行榜） ──
# 默认走 AI 网关（= AI_GATEWAY_URL）。仅在需绕过网关单服务直连时填写。
# 单服务直连 prod: https://web3-data-api-prod.gateweb3.cc
# DATA_API_URL=

# ── BIZ_WALLET_URL ── web3-business-wallet 登录前置流程入口 ──
# 用途：OAuth Device Flow（/v1/wallet/oauth/{gate|google}/device/{start|poll}）
#       + merchant 凭证拉取（/v1/wallet/config/merchant）
# 默认走 AI 网关（= {AI_GATEWAY_URL}/web3-business-wallet）。
# 单服务直连 prod: https://webapi.gateweb3.cc/api/web/v1/web3-business-wallet
# 注意：网关前缀是 /api/v1/...，直连测试/prod URL 历史上多一段 web —— 不要混。
# BIZ_WALLET_URL=
