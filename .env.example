# gate-dex-cli 环境变量配置示例
# 复制此文件为 .env 后按需填值；所有变量都可不填，留空时走源码 prod 默认值
#
# 用法：
#   cp .env.example .env
#   # 编辑 .env 取消注释并填入所需 URL
#
# ⚠️ BIZ_WALLET_URL vs BW_SERVICE_URL 注意事项（不能合并）
#   - BIZ_WALLET_URL  = webapi 网关入口（登录 OAuth Device Flow + merchant 凭证）
#   - BW_SERVICE_URL  = 直连 BW 业务服务（登录后所有钱包/交易/签名调用）
#   两者由后端分别承载：网关侧带 WAF/风控 + cookie 域名绑定；直连侧低延迟。
#   合并会破坏 OAuth callback 域名校验，并增加路径前缀切换复杂度。
#   详细对比见 cli/src/core/api-client.ts 头部注释。

# ── GV (Gate Verify) API ────────────────────────────────────
# 默认 prod: https://api.web3gate.io/api/plug/v1/web3-gv-api
# GV_URL=

# ── 动态域名 CDN 候选列表（逗号分隔；覆盖默认 prod 列表） ──────────
# 默认 prod: 源码内置 7 个 prod CDN host（freshmarkethome / freshmarketpage /
#           gateweb3.io / ldd678 / web3gate.cc / web3gate.io / web3-wallet-cdn-prod）
# CDN_DOMAINS=

# ── web3_domain host 列表（逗号分隔；设置后跳过 CDN 拉取直接使用） ──
# 默认 prod: 留空，走 CDN_DOMAINS 动态拉取
# 仅 pre 环境需要硬编码：
#   pre: http://pre-api.ldd710.com,http://pre-api.ldd711.com,http://pre-api.ldd712.com
# WEB3_DOMAIN_HOSTS=

# ── gateio-service-web3-wallet（balance 命令用） ──────────────
# 默认 prod: https://web3-wallet-service-prod.gateweb3.cc
# WALLET_SERVICE_URL=

# ── BW_SERVICE_URL ── web3-business-wallet 直连地址（登录后业务调用） ──
# 用途：钱包/转账/签名等所有业务接口
# ⚠️ 服务注册中心未暴露 BW prod 公网域名，源码默认值为内网入口：
#   http://web3-ingress-prod.gateweb3.io/web3-business-wallet
# 公网部署务必显式注入实际可达地址。
# BW_SERVICE_URL=

# ── gateio_service_web3_trade_token (市场行情 / Token 列表 / Swap) ──
# 默认 prod: https://apipro-new.gateweb3.cc（待运维确认）
# MARKET_TOKEN_URL=

# ── gateio web3-data-api (Token 详情 / 安全审计 / 排行榜) ─────
# 默认 prod: https://web3-data-api-prod.gateweb3.cc
# DATA_API_URL=

# ── BIZ_WALLET_URL ── web3-business-wallet 走 webapi 网关的入口（登录前置流程） ──
# 用途：OAuth Device Flow（/v1/wallet/oauth/{gate|google}/device/{start|poll}）
#       + merchant 凭证拉取（/v1/wallet/config/merchant）
# 默认 prod: https://webapi.gateweb3.cc/api/web/v1/web3-business-wallet
# 注：pre 与 prod 共用同一域名（webapi.gateweb3.cc）
# BIZ_WALLET_URL=
